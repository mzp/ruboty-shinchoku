require 'spec_helper'

describe Ruboty::Shinchoku do
  it 'has a version number' do
    expect(Ruboty::Shinchoku::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
